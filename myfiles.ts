type ShopOfNumbers = { 
    name : string
    items : Array<number>
}
type ShopOfStrings = { 
    name : string
    items : Array<string>
}
type ShopOfBooleans = { 
    name : string
    items : Array<boolean>
}